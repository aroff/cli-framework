[package]
name = "tower_compat"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
axum = { version = "0.8" }
tower_real = { package = "tower", version = "0.5", features = ["util"] }
tower-layer = "0.3"
tower-service = "0.3"

