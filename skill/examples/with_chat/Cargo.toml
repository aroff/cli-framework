[package]
name = "with_chat"
version = "0.1.0"
edition = "2021"

[dependencies]
cli-framework = { path = "../../../", features = ["chat"] }
anyhow = "1.0"
tokio = { version = "1.0", features = ["full"] }
