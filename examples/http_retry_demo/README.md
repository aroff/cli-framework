# `http_retry_demo`

Exercises **`RetryableHttpClient`** against **https://httpbin.org** (network required). Shows retry behavior on success and failing status codes.

## Run

From the repository root:

```bash
cargo run --example http_retry_demo
```

## See also

README [Secure HTTP client](../../README.md#secure-http-client) and `cli_framework::http_retry`.
