//! Validation is now handled directly by `CommandSpec::validate_typed_args`.
//! This module is retained as an empty placeholder to avoid breaking any
//! `use crate::parser::validator` paths; it will be removed in a future cleanup.
